export function findCharacterId() {
  //let ids = localStorage.characterIds.sort()
  // const characterId = ids.reduce((final, num) => {
  //   if (num === final) final++
  //   return final
  // }, 1)
  // if (localStorage.characterIds) localStorage.characterIds.push(characterId)
  // else localStorage.characterIds = [characterId]
  return 1
}
